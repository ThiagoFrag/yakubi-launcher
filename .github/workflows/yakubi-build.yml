name: Yakubi Build

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  test:
    name: Test runner
    runs-on: windows-2022
    steps:
      - name: Hello
        run: echo "Runner works"
